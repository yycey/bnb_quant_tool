# BNB 量化交易工具 — 已实现功能总结

> 基于当前代码库整理（系统 **v3.0.0**）  
> 最后更新：2026-08-14  
> 配套文档：[README.md](./README.md)、[web/README.md](./web/README.md)（Web 部署）

---

## 一、工具定位

**这是一个 AI 辅助的 BNB 量化分析与模拟交易系统**，不是全自动实盘机器人。

| 维度 | 说明 |
|------|------|
| 核心目标 | 给出可执行的开单建议（方向 / 入场 / 止损 / 分批止盈 / 仓位），并通过模拟盘 + AI 学习持续进化 |
| 交易对象 | 默认 `BNBUSDT`，主周期 `1h`（可配置） |
| 实盘方式 | 用户在交易所**手动下单**；工具提供建议与模拟验证 |
| 数据依赖 | Binance 公开 K 线（支持镜像自动切换）；合约情绪部分走 Gate.io；新闻走 BlockBeats |
| AI 依赖 | DeepSeek API（市场解读、新闻摘要、自动复盘） |

---

## 二、整体架构

```
┌─────────────────────────────────────────────────────────────┐
│  交互层                                                       │
│  gui.py (14 Tab) │ main.py (CLI) │ web/ (PHP 控制台)         │
│  paper_watcher.py │ data_manager_gui.py │ discover_strategies │
└──────────────────────────┬──────────────────────────────────┘
                           │
┌──────────────────────────▼──────────────────────────────────┐
│  决策层                                                       │
│  trade_advisor · trading_signals · institutional_strategies   │
│  circuit_breaker · pattern_memory · decision_explainer        │
│  dynamic_position · market_regime                             │
└──────────────────────────┬──────────────────────────────────┘
                           │
┌──────────────────────────▼──────────────────────────────────┐
│  分析层                                                       │
│  ai_analyzer · technical_indicators · multi_timeframe         │
│  market_sentiment · news_collector · backtest_engine          │
│  deep_learning_engine · indicator_explorer · strategy_lab     │
└──────────────────────────┬──────────────────────────────────┘
                           │
┌──────────────────────────▼──────────────────────────────────┐
│  执行 / 数据层                                                │
│  data_fetcher · paper_trading · price_alert · grid_strategy │
│  signal_scanner · risk_manager · data_localization            │
└──────────────────────────┬──────────────────────────────────┘
                           │
┌──────────────────────────▼──────────────────────────────────┐
│  学习与进化层                                                 │
│  ai_learning_system · counterfactual_analyzer · trade_quality │
│  ai_review_engine · param_manager · strategy_mutator          │
│  ai_trading_context                                           │
└─────────────────────────────────────────────────────────────┘
```

所有 SQLite 数据库统一存放在 `data/` 目录（`data_localization.py` 管理路径、导出/迁移）。

---

## 三、启动入口

| 入口 | 文件 | 用途 |
|------|------|------|
| **生产唯一入口** | `启动服务器.bat` → `autopilot_daemon.py` | 分析 + 开仓 + 内嵌盯仓（一个进程） |
| 图形界面（调试） | `gui.py` | 本机调试，勿与服务器同开 |
| 命令行 | `main.py` | 轻量分析，输出 JSON/文本 |
| 模拟盘盯仓（调试） | `paper_watcher.py` | 仅调试；生产已内嵌，勿另开 |
| Web 控制台 | `web/start_dev.bat` | 本机面板 `127.0.0.1:8787` |
| 策略发现 | `discover_strategies.py` | StrategyLab |

已删除：`启动.bat` / `启动监控.bat` / `启动web.bat`（避免多入口抢单、双写）。

---

## 四、GUI 功能（14 个 Tab）

| # | Tab | 已实现功能 |
|---|-----|-----------|
| 0 | 💰 开单建议 | 一键分析结果：方向、入场区间、SL、TP1/2/3、置信度、仓位、可复制下单文本 |
| 1 | 分析摘要 | 多指标综合摘要 |
| 2 | 机构策略(13个) | 各策略投票明细、共识方向与置信度 |
| 3 | 原始数据(JSON) | 完整分析结果 JSON，便于调试 |
| 4 | AI 学习 | 学习仪表盘、待反馈列表、学习报告、策略权重 |
| 5 | 网格策略 | 网格参数生成与挂单建议（参数层，非完整自动网格交易） |
| 6 | 📊 回测 | 历史 K 线回测：胜率、回撤、夏普等（含手续费/滑点，不含 AI 调用） |
| 7 | 市场情绪 | 恐惧贪婪指数、资金费率、持仓量、多周期共振 |
| 8 | 🔔 价格预警 | 价格上下穿规则、15 秒轮询 |
| 9 | 📰 新闻情报 | BlockBeats 新闻抓取 + DeepSeek 情感/摘要 |
| 10 | 💰 模拟交易 | 自动跟单、持仓列表、手动平仓、统计、训练包导出/导入 |
| 11 | 🧠 AI 复盘 | 手动/自动复盘、参数建议、一键应用、策略变异入口 |
| 12 | 📋 信号追踪 | 记录真实交易结果，与 AI 分析 record_id 关联，闭环反馈 |
| 13 | 📡 信号扫描 | 主动行情扫描、信号列表、触发 AI 全自动 |

### GUI 顶部控制区

- **开始分析**：执行完整 8 步分析流水线
- **AI 全自动**：定时循环「分析 → 门控 → 模拟开仓 → 复盘」
- **定时分析**（`auto_run`）：默认关闭，可配置间隔（默认 60 分钟）
- **信号扫描开关**：启动/停止 SignalScanner 后台线程

---

## 五、核心分析流水线（8 步）

GUI 主分析路径（`gui.py`）：

| 步骤 | 内容 |
|------|------|
| Step 1 | 拉取主周期 K 线（`BinanceDataFetcher`，复用 `self.fetcher`） |
| Step 2 | 计算技术指标（RSI、MACD、布林带、MA、ATR 等） |
| Step 3 | 识别市场状态（7 种 Regime）+ 运行机构策略加权投票 |
| Step 4 | 并行等待：新闻、情绪、多周期后台任务 |
| Step 5 | DeepSeek 总结新闻利好/利空（有新闻且配置了 API Key 时） |
| Step 6 | DeepSeek 主市场分析 |
| Step 7 | 融合技术信号、交易计划、风控参数 |
| Step 8 | `TradeAdvisor` 生成最终开单建议（含门控、动态仓位、决策解释） |

分析完成后自动：

- 写入 `ai_learning.db`（`record_analysis`）
- 可选自动跟单（`paper_trading.auto_follow`）
- 展示 `DecisionExplainer` 加减分明细

---

## 六、决策与门控体系

### 6.1 TradeAdvisor 输出

每次分析产出一份结构化建议：

- **方向**：`LONG` / `SHORT` / `WAIT`
- **原始方向**：`raw_action`（门控前）
- **价格**：入场区间、止损、TP1/TP2/TP3
- **仓位**：USDT 金额 + BNB 数量
- **质量指标**：置信度、信号强度、风险回报比、有效期
- **决策解释**：各因子加减分（`decision_explainer`）

### 6.2 门控过滤器（多层叠加）

| 层级 | 模块 | 规则概要 |
|------|------|---------|
| 多周期 | `multi_timeframe` | 15m/1h/4h/1d 共振过滤 |
| 新闻 | `trade_advisor` | 极端利空/利好时拦截 |
| 时段 | `trade_advisor` | UTC 小时白名单（基于历史统计） |
| 连亏 | `trade_advisor` | 连亏达阈值缩小仓位或停单 |
| 基础门控 | `_gate_check` | 最低置信度、RR、学习成熟度、模拟盘胜率 |
| 熔断器 | `circuit_breaker` | 连亏 3 笔减半仓；5 笔停单；24h 回撤 >8% 停单；波动异常缩仓 |
| 模式记忆 | `pattern_memory` | 相似历史局面胜率 <35% 且样本 ≥5 时拦截 |
| AI 纪律 | `ai_trading` | `require_gate_pass: true` 时禁止强开；`use_relaxed_mode: false` 禁止用 raw_action 绕过 WAIT |
| 持仓上限 | `trade_advisor` | `max_open_positions` 检查 |

### 6.3 市场状态引擎（`market_regime.py`）

识别 7 种状态并动态调整机构策略权重：

`TRENDING` · `RANGING` · `HIGH_VOLATILITY` · `LOW_VOLATILITY` · `NEWS_DRIVEN` · `PANIC` · `EUPHORIA`

### 6.4 动态仓位（`dynamic_position.py`）

在基础仓位上乘以多因子系数（0.25～1.25），考虑：

- AI 置信度、波动率、多周期共振、新闻风险
- 市场状态、熔断缩仓因子、连亏/连胜

### 6.5 自适应止损/止盈（`trade_advisor` v2.0+）

- 低波动 ATR 倍数 1.3x → 高波动 2.2x
- TP 分批比例：TP1 40% / TP2 35% / TP3 25%（config 可改）
- 深度学习信号可选融合（`dl_weight: 0.25`）

---

## 七、机构策略（13 个内置 + 自动发现）

`institutional_strategies.py` 内置策略：

| 类别 | 策略 |
|------|------|
| 技术指标 | SMA 交叉、EMA 交叉、布林带、RSI 极值、MACD 交叉、斐波那契 |
| 机构风格 | Renaissance 统计套利、Citadel 动量、Bridgewater 风险平价、AQR 价值动量、Two Sigma ML、Jump 做市、海龟法则 |

额外支持：

- **StrategyLab 自动发现策略**：从 `data/models/strategy_library.json` 加载，注册为 `auto_*` 策略
- **AI 学习权重**：`ai_learning_system` 根据历史表现动态调整各策略权重
- **市场状态加权**：Regime 检测器再乘以状态系数

---

## 八、AI 能力

| 能力 | 模块 | 说明 |
|------|------|------|
| 市场解读 | `ai_analyzer` | DeepSeek 分析 K 线 + 指标，输出方向与理由 |
| 新闻摘要 | `ai_analyzer` + `news_collector` | BlockBeats 新闻 + AI 情感/摘要 |
| 模拟盘上下文 | `ai_trading_context` | 将真实模拟盘绩效注入提示词 |
| 决策解释 | `decision_explainer` | 人类可读的因子分解，写入 DB 并在 Web 展示 |
| 自动复盘 | `ai_review_engine` | 每 N 笔平仓或连亏触发，AI 建议参数调整 |
| 参数版本管理 | `param_manager` | 配置变更记录、应用、回滚 |
| 策略变异 | `strategy_mutator` | AI 驱动策略变异 + 影子测试 |
| 深度学习投票 | `deep_learning_engine` | 自研 numpy 预测器，可选参与信号融合 |
| 指标探索 | `indicator_explorer` | 遗传算法探索指标组合（研究用途） |
| 策略实验室 | `strategy_lab` | DSL 策略发现，输出到 strategy_library |

DeepSeek 配置支持 `thinking.type: enabled` 与 `reasoning_effort: high`。

---

## 九、AI 学习闭环

### 9.1 数据库（`data/ai_learning.db`）

| 表 | 用途 |
|----|------|
| `analysis_records` | 每次分析快照（含 decision_explanation、gate_reasons、raw_action、passed_gate） |
| `strategy_performance` | 13+ 策略的历史胜率与权重 |
| `learning_log` | 学习事件流水 |
| `market_snapshots` | 市场快照 |
| `param_change_log` | 参数变更历史 |

### 9.2 反馈链路

```
分析 → record_analysis (拿 record_id)
  → 模拟开仓 (learning_record_id 关联)
  → 平仓 → submit_feedback (WIN/LOSS + R-multiple)
  → 策略权重更新
  → 反事实分析 (NO_TRADE / REVERSE / LATE_ENTRY)
  → 交易质量评分 (MFE/MAE/RR)
  → 模式记忆指纹入库
  → 触发 AI 复盘 → 参数自动应用 (auto_apply)
```

### 9.3 信号追踪闭环

- 开仓时记录 `signal_tracking_id`
- 「信号追踪」Tab 可回填真实交易结果
- 平仓时自动关联并反馈到学习系统

---

## 十、模拟交易（`paper_trading.py`）

### 10.1 核心能力

- 接收 `TradeAdvisor` 建议自动开仓
- 后台 watcher 轮询价格（默认 15 秒），自动检查 SL/TP
- SQLite 持久化（`data/paper_trading.db`）
- 统计：胜率、盈亏比、累计收益、最大回撤、连亏等

### 10.2 分批止盈规则

| 事件 | 动作 |
|------|------|
| 触及 TP1 | 平仓一部分（默认 40%），SL 移到入场价（保本） |
| 触及 TP2 | 再平一部分（默认 35%），SL 移到 TP1（锁利） |
| 触及 TP3 | 平掉剩余（默认 25%） |
| 触及 SL | 全平剩余 |
| 持仓超时 | 超过 `max_position_age_hours`（默认 24h）市价平仓 |
| 手动平仓 | GUI / Web 触发 |

### 10.3 质量追踪

每笔平仓记录：

- MFE / MAE（最大浮盈/浮亏）
- R-multiple（最终 PnL / 初始风险）
- 平仓原因（止损、止盈、超时、手动等）

手续费：双边 0.04%（模拟币安期货吃单费率）。

---

## 十一、自动化模式

| 模式 | 触发方式 | 行为 |
|------|---------|------|
| 手动分析 | 点击「开始分析」 | 完整 8 步 → 可选 auto_follow 开仓 |
| 定时分析 | `auto_run.enabled: true` | 按 `interval_minutes` 定时执行分析 |
| AI 全自动 | GUI 勾选 | 循环：分析 → 全部门控 → 模拟开仓 → 定期复盘 |
| 信号扫描 | SignalScanner 线程 | 60s 轻量扫描，强信号触发 fullauto |
| 模拟自动跟单 | `paper_trading.auto_follow: true` | 每次分析通过后自动 paper 开仓 |
| 后台监控 | `paper_watcher.py` 或 GUI 内置 | 15s 检查 SL/TP/超时 |

### SignalScanner 触发条件（6 种）

1. RSI 穿越超卖/超买区  
2. 布林带触边  
3. 成交量突增（>2x 均量）  
4. 价格突破近 N 根高低点  
5. 多周期共振（15m/1h/4h，带 5 分钟缓存）  
6. 单根 K 线涨跌幅 > 1.5x ATR  

防噪：冷却期 300s、触发间隔 120s、最低强度 0.6。

---

## 十二、回测引擎（`backtest_engine.py`）

- 历史 K 线回放，不含 DeepSeek 调用（控制成本）
- 支持手续费（0.04%）与滑点（0.05%）
- 输出胜率、最大回撤、夏普比率等
- GUI「回测」Tab 可交互运行；参数对比功能

> 注意：回测使用单一 TP/SL 逻辑，与模拟盘三级分批止盈存在差异；同一 K 线内 TP/SL 先后顺序为估算。

---

## 十三、Web 控制台（`web/`）

PHP + SQLite 远程面板，Python 引擎不变。

### 13.1 页面 Tab（8 个）

持仓 · 历史 · 信号追踪 · AI 学习 · 绩效 · 监控 · 维护 · 配置

### 13.2 主要 API

| 类型 | 端点 | 功能 |
|------|------|------|
| 读 | overview, market, positions, history, signals | 总览与交易数据 |
| 读 | ai_learning, strategies, performance | 学习与策略权重 |
| 读 | latest_advice, decision_history | 最新 AI 建议 + 决策解释 |
| 读 | monitor, status | watcher 心跳、系统状态 |
| 读/写 | config / config_update | 远程查看/修改配置（自动备份） |
| 写 | close_position | Web 手动平仓 |
| 维护 | maintenance, maintenance_upload | 健康检查、修复、备份、zip 热更新、Git 拉取 |

### 13.3 运维能力（`system_maintenance.py`）

- 健康检查（DB schema、Python 模块、Web 文件）
- 自动修复（列迁移、根目录 DB 迁入 data/）
- SQLite VACUUM + WAL checkpoint
- 打包备份 config + data/*.db
- zip 热更新代码（不覆盖 data/ 与 config）

安全：`web.api_token` 设置后所有 API 需 `Authorization: Bearer <token>`。

---

## 十四、数据与行情

### 14.1 Binance 数据（`data_fetcher.py`）

- K 线、Ticker、订单簿、最近成交
- 镜像自动探测与回退：`api.binance.me` → `api.binance.com`
- 启动时 `_probe_mirror()` 选择可用端点

### 14.2 市场情绪（`market_sentiment.py`）

- 恐惧贪婪指数
- 资金费率、持仓量（合约端，Gate.io API）
- 综合情绪分数

### 14.3 新闻（`news_collector.py`）

- BlockBeats API 抓取 BNB 相关新闻
- 关键词情感 + DeepSeek 深度摘要

### 14.4 价格预警（`price_alert.py`）

- 上下穿规则
- 15 秒轮询（Binance 镜像）
- 通知渠道配置存在但默认关闭（email/webhook）

### 14.5 数据本地化（`data_localization.py`）

- 统一 DB 路径到 `data/`
- 导出/导入/迁移工具（`data_manager_gui.py`）

### 14.6 数据库文件

| 文件 | 用途 |
|------|------|
| `data/paper_trading.db` | 模拟持仓与成交 |
| `data/ai_learning.db` | AI 学习与分析记录 |
| `data/pattern_memory.db` | 模式记忆指纹 |
| `data/counterfactual.db` | 反事实分析结果 |
| `data/watcher.heartbeat` | 监控进程心跳 |

---

## 十五、配置体系（`config.yaml`）

统一读取入口：`config_access.py`（`build_trade_advisor_config()` 等）。

| 配置节 | 作用 |
|--------|------|
| `deepseek` | AI API |
| `binance` | 交易所 API（公开数据可不填 Key） |
| `trading` | 交易对、周期、置信度门槛、账户余额 |
| `risk_management` | 最大仓位、RR、持仓上限、日/周亏损上限 |
| `multi_timeframe` | 多周期开关与权重 |
| `sentiment` | 情绪刷新间隔 |
| `price_alert` | 预警轮询 |
| `paper_trading` | 模拟盘、auto_follow、超时、relaxed_mode |
| `auto_review` | AI 复盘触发条件与 auto_apply |
| `auto_run` | 定时分析 |
| `backtest` | 回测默认参数 |
| `trade_advisor` | SL/TP 倍数、时段过滤、连亏、TP 分批、DL 权重 |
| `analysis` | 指标列表 |
| `news` | BlockBeats |
| `notifications` | 邮件/Webhook（默认关） |
| `signal_scanner` | 扫描器参数 |
| `ai_trading` | 全自动纪律（gate_pass、pattern_memory） |
| `web` | Token、Python 路径、在线更新 |

以下模块有代码默认值，但 config 中**尚未显式写出**（可按需补充）：

- `circuit_breaker`
- `market_regime`
- `dynamic_position`

---

## 十六、反事实学习与模式记忆

### CounterfactualAnalyzer

每笔平仓后异步计算：

- **NO_TRADE**：如果不交易会怎样
- **REVERSE**：如果反向做会怎样
- **LATE_ENTRY**：如果晚 3 小时进场会怎样

结果持久化到 `data/counterfactual.db`。

### PatternMemory

- 10 维特征指纹（RSI、BB 位置、MACD、量能、AI 置信度等）
- 余弦相似度匹配历史局面
- 全自动模式下胜率过低则拦截开仓
- 首次启动自动回填已有平仓记录

---

## 十七、研究型模块（已实现，偏实验）

| 模块 | 状态 | 说明 |
|------|------|------|
| `strategy_lab` | ✅ 可用 | DSL 策略发现，CLI + 自动注册到机构策略 |
| `strategy_mutator` | ✅ 可用 | AI 变异策略，GUI 入口在 AI 复盘 Tab |
| `indicator_explorer` | ⚠️ 部分 | 遗传探索可用，回测 fitness 为启发式（非完整信号回测） |
| `deep_learning_engine` | ⚠️ 部分 | numpy 自研 LSTM/DQN，数据不足时预测质量有限 |
| `grid_strategy` | ⚠️ 部分 | 参数生成可用，未接完整自动网格交易流程 |

---

## 十八、CLI 与 GUI 差异

| 能力 | GUI | main.py |
|------|-----|---------|
| 完整 8 步分析 | ✅ | ✅（简化版） |
| 机构策略 + Regime | ✅ | ✅ |
| 熔断 / 模式记忆 | ✅ | ❌（导入但未接入 analyze） |
| 信号扫描 | ✅ | ❌ |
| 模拟交易 | ✅ | ❌ |
| AI 复盘 / 参数管理 | ✅ | ❌ |
| 决策解释写入 DB | ✅ | 部分 |

`paper_watcher.py` 仅负责持仓监控，配置读取路径与 GUI 的 `build_trade_advisor_config()` 不完全一致。

---

## 十九、设计边界（ intentionally 未实现）

以下能力**不在当前设计范围**，避免误解：

| 项 | 说明 |
|----|------|
| 实盘自动下单 | 无 Binance 签名下单模块；定位是辅助手动交易 |
| 多币种并行 | 配置可改 symbol，但 workflow 为单交易对 |
| 高频 / 毫秒级 | 扫描 60s、分析 30～60min 级，趋势系统 |
| Monte Carlo / Walk Forward | ROADMAP 规划项，代码未实现 |
| 新闻衰减 / 宏观联动 | ROADMAP 规划项，代码未实现 |
| 完整 PyTorch 深度学习 | 使用轻量 numpy 实现 |

---

## 二十、模块清单（`src/bnb_quant_tool/`）

```
ai_analyzer.py          ai_learning_system.py    ai_review_engine.py
ai_trading_context.py   backtest_engine.py       circuit_breaker.py
config_access.py        counterfactual_analyzer.py  data_fetcher.py
data_localization.py    decision_explainer.py    deep_learning_engine.py
dynamic_position.py     grid_strategy.py         indicator_explorer.py
institutional_strategies.py  market_regime.py    market_sentiment.py
multi_timeframe.py      news_collector.py        paper_trading.py
param_manager.py        pattern_memory.py        price_alert.py
risk_manager.py         signal_scanner.py        strategy_lab.py
strategy_mutator.py     system_maintenance.py    technical_indicators.py
trade_advisor.py        trade_quality.py         trading_signals.py
```

共 **34** 个模块 + 根目录 `gui.py`、`main.py`、`paper_watcher.py` 等入口脚本。

---

## 二十一、典型使用流程

```
1. 配置 config.yaml（API Key、账户余额、门槛参数）
2. 双击 启动服务器.bat（一个进程：分析+开仓+盯仓）
3. 可选：web\start_dev.bat 看控制台
4. 改配置后重启 启动服务器.bat / 计划任务即可
```

---

## 二十二、版本信息

| 组件 | 版本 |
|------|------|
| 系统包 `__version__` | **3.0.0** |
| GUI / Web / Autopilot | v3.0 |
| 关键升级 | 探针 SL/TP 纠正 · 熔断冷却恢复 · 连亏 ignore 对齐 · 软超时保浮盈 · 验证 LONG 加严放宽 |

---

*本文档描述的是代码中已实现并可用的功能。ROADMAP 中规划但未落地的项见 [ROADMAP.md](./ROADMAP.md)。*
